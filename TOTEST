
crane pull  - ?
docker save - ?
ctr export  - tested

from those:

1) docker run
2) run workload in the cluster

STEPS: (requires "HDR: X-Registry, VALUES: docker.io")

/etc/containerd/certs.d/docker.io/hosts.toml:
=============================================
server = "http://192.168.0.49:8080"

[host."http://192.168.0.49:8080"]
  capabilities = ["pull"]
  skip_verify = true
  [host."http://192.168.0.49:8080".header]
    X-Registry = ["docker.io"]
