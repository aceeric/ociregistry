# helm upgrade --install ociregistry ociregistry\
#  --namespace ociregistry\
#  --create-namespace\
#  --set image.ociregistry.tag=1.8.3-test\
#  --set serverTls.enabled='true'\
#  --set serverTls.clientAuth='verify'\
#  --set-file serverTls.cert=/tmp/localhost.crt\
#  --set-file serverTls.key=/tmp/localhost.key\
#  --set-file serverTls.ca=/tmp/localhost.crt\
#  --dry-run=server

# curl --cert /tmp/localhost.crt --key /tmp/localhost.key\
#      --cacert /tmp/localhost.crt https://192.168.122.147:31080/v2/registry.k8s.io/kube-scheduler/manifests/v1.29.1

# helm upgrade --install ociregistry ociregistry\
#  --namespace ociregistry\
#  --create-namespace\
#  --values ./test-values.yml\
#  --dry-run=server
ingress:
  enabled: true
  hosts:
  - host: dtk.io
    paths:
    - path: /
      pathType: ImplementationSpecific

serverConfig:
  config:

service:
  type: ClusterIP
  nodePort:
